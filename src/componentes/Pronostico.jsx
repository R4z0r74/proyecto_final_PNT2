import React from 'react'

const Pronostico = ({ pronostico }) => {
    console.log(pronostico.list);

  return (
    <>
        <h3>Pronostico de los proximos dias</h3>
    </>
  )
}

export default Pronostico